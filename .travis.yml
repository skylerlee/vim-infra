language: generic
services:
  - docker
before_install:
  - docker pull testbed/vim
  - docker run testbed/vim install_vim -tag master -name vim-master -build
script:
  - docker run -it --rm testbed/vim vim-master --version
